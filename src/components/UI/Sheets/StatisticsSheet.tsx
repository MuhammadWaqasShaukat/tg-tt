import { Button } from "../Buttons";

const StatisticsSheet = () => {
  return (
    <Button
      acent="yellow"
      className="shadow-custom flex flex-row justify-center items-center gap-2 text-[1.13em] rounded-2xl"
    >
      <span>Spread the News :)</span>
    </Button>
  );
};

export default StatisticsSheet;
